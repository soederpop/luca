# Building your own endpoints

Endpoints are single module typescript files which export functions which handle 
web requests, along with metadata about that endpoint to generate API documentation.
Luca's ExpressServer helper can load these endpoints and serve them.
