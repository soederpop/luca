# Main Server

Want to build a server with luca that I expose through the cloudflare tunnel, that runs inside a docker container on my machine

This will be the persistent server for luca, and act as a CDN and other things for which I need a stable URL

It will serve static html assets for frontend web features