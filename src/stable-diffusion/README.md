# StableDiffusion Interface

This interface assumes you have a checkout of automatic1111 stable diffusion webui.

The [generate.py](generate.py) in this folder can be run in the `cwd` of the checkout.

It outputs JSON that we can use the `container.proc` feature and just invoke it from javascript
